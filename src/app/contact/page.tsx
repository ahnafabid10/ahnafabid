import ContactForm from '@/components/HomePage/ContactForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <ContactForm/>
        </div>
    );
};

export default page;